---
import { Image } from 'astro:assets';
import { Icon } from 'astro-icon/components';
import { testimonials, testimonialsIntro } from '../data/content';

const hasCarousel = testimonials.length > 1;
---

<section class="hairline py-16 lg:py-24" aria-labelledby="testimonios-titulo">
  <div class="container-page">
    <div class="grid gap-8 lg:grid-cols-[1fr_auto] lg:items-end lg:gap-16">
      <div>
        <p class="label-mono text-muted">Testimonios</p>
        <h2 id="testimonios-titulo" class="heading-xl mt-6 max-w-[18ch] text-ink text-balance">
          {testimonialsIntro.title}
        </h2>
      </div>
      <p class="max-w-sm font-serif text-lg italic text-muted lg:pb-2">
        {testimonialsIntro.verse}<br />
        {testimonialsIntro.reference}
      </p>
    </div>

    <div
      id="testimonios-track"
      aria-roledescription={hasCarousel ? 'carrusel' : undefined}
      aria-label={hasCarousel ? 'Testimonios' : undefined}
      tabindex={hasCarousel ? 0 : undefined}
      class:list={[
        'no-scrollbar mt-12 flex gap-6 overflow-x-auto scroll-smooth lg:mt-16',
        hasCarousel && 'snap-x snap-mandatory',
      ]}
    >
      {
        testimonials.map((testimonial) => (
          <figure
            class="relative w-full shrink-0 snap-center overflow-hidden rounded-media bg-carbon"
          >
            <Image
              src={testimonial.poster}
              alt={testimonial.posterAlt}
              widths={[640, 960, 1280]}
              sizes="(min-width: 64rem) 1248px, 100vw"
              class="aspect-[16/9] w-full object-cover lg:aspect-[2/1]"
              loading="lazy"
              decoding="async"
            />
            <div
              class="absolute inset-0 bg-gradient-to-r from-black/80 via-black/45 to-transparent"
              aria-hidden="true"
            />

            <div class="absolute inset-0 flex flex-col justify-center gap-4 p-6 sm:p-10 lg:max-w-[62%] lg:p-12">
              <div class="flex items-center gap-4">
                <Image
                  src={testimonial.thumb}
                  alt={testimonial.thumbAlt}
                  width={112}
                  height={80}
                  densities={[1, 2]}
                  class="hidden h-20 w-28 rounded-xl object-cover sm:block"
                  loading="lazy"
                  decoding="async"
                />
                <figcaption class="label-mono text-paper/70">{testimonial.name}</figcaption>
              </div>

              <blockquote class="heading-lg max-w-[20ch] text-paper text-balance">
                {testimonial.quote}
              </blockquote>
            </div>
          </figure>
        ))
      }
    </div>

    {
      hasCarousel && (
        <div class="mt-8 flex items-center justify-center gap-3">
          <button
            type="button"
            data-carousel-prev
            class="grid size-12 place-items-center rounded-full bg-pill text-ink transition-colors hover:bg-pill-strong disabled:cursor-default disabled:opacity-40 disabled:hover:bg-pill"
          >
            <span class="sr-only">Testimonio anterior</span>
            <Icon name="lucide:arrow-left" width={18} height={18} aria-hidden="true" />
          </button>
          <button
            type="button"
            data-carousel-next
            class="grid size-12 place-items-center rounded-full bg-pill text-ink transition-colors hover:bg-pill-strong disabled:cursor-default disabled:opacity-40 disabled:hover:bg-pill"
          >
            <span class="sr-only">Testimonio siguiente</span>
            <Icon name="lucide:arrow-right" width={18} height={18} aria-hidden="true" />
          </button>
        </div>
      )
    }
  </div>
</section>

<script>
  const track = document.getElementById('testimonios-track');
  const prev = document.querySelector<HTMLButtonElement>('[data-carousel-prev]');
  const next = document.querySelector<HTMLButtonElement>('[data-carousel-next]');

  if (track && prev && next) {
    const step = () => track.clientWidth + 24; // ancho visible + gap

    prev.addEventListener('click', () => track.scrollBy({ left: -step(), behavior: 'smooth' }));
    next.addEventListener('click', () => track.scrollBy({ left: step(), behavior: 'smooth' }));

    // Apagar la flecha cuando ya no queda nada hacia ese lado.
    const syncArrows = () => {
      const max = track.scrollWidth - track.clientWidth;
      prev.disabled = track.scrollLeft <= 1;
      next.disabled = track.scrollLeft >= max - 1;
    };

    track.addEventListener('scroll', syncArrows, { passive: true });
    window.addEventListener('resize', syncArrows);
    syncArrows();
  }
</script>
